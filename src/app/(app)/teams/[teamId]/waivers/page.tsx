import { notFound } from "next/navigation";
import { Trash2 } from "lucide-react";
import { requireUser } from "@/lib/auth/session";
import { db } from "@/lib/db";
import { addWaiverCandidate, removeWaiverCandidate } from "@/features/waivers/actions";
import { generateWaivers } from "@/features/recommendations/actions";
import { FormSubmit } from "@/components/form-submit";
import { TeamNav } from "@/components/team-nav";
import { getFantasyWeek } from "@/lib/fantasy-week";

export default async function WaiversPage({params}:{params:Promise<{teamId:string}>}) { const {season:SEASON,week:WEEK}=getFantasyWeek(); const user=await requireUser(); const {teamId}=await params; const team=await db.team.findFirst({where:{id:teamId,userId:user.id},include:{waiverCandidates:{orderBy:{projectedPoints:"desc"}},waiverRecommendations:{where:{season:SEASON,week:WEEK},orderBy:{priorityScore:"desc"},take:10}}}); if(!team)notFound(); return <main className="mx-auto max-w-6xl p-5 md:p-8"><p className="text-sm text-emerald-300">{team.name} · Week {WEEK}</p><h1 className="mb-5 text-3xl font-bold">Waiver advisor</h1><TeamNav teamId={team.id} active="waivers"/><div className="grid gap-5 lg:grid-cols-[1fr_350px]"><section className="space-y-4"><div className="card p-5"><div className="mb-4 flex items-center justify-between"><h2 className="font-bold">Available players</h2><form action={generateWaivers}><input type="hidden" name="teamId" value={team.id}/><input type="hidden" name="season" value={SEASON}/><input type="hidden" name="week" value={WEEK}/><FormSubmit pending="Ranking…">Rank waivers</FormSubmit></form></div>{team.waiverCandidates.map(p=><div key={p.id} className="grid grid-cols-[1fr_70px_35px] border-t border-[#202a38] py-3 text-sm"><span><b>{p.name}</b><small className="ml-2 muted">{p.position} · {p.nflTeam}</small></span><b>{p.projectedPoints.toFixed(1)}</b><form action={removeWaiverCandidate}><input type="hidden" name="teamId" value={team.id}/><input type="hidden" name="candidateId" value={p.id}/><button className="text-red-300"><Trash2 size={15}/></button></form></div>)}</div>{team.waiverRecommendations.length>0&&<div className="card p-5"><h2 className="mb-3 font-bold">Top pickups</h2>{team.waiverRecommendations.map((r,i)=><div className="border-t border-[#202a38] py-3 text-sm" key={r.id}><div className="flex justify-between"><b>{i+1}. {(r.addPlayer as {name:string}).name}</b><span className="text-emerald-300">Priority {r.priorityScore.toFixed(0)}</span></div><p className="mt-1 muted">{r.explanation}</p></div>)}</div>}</section><aside className="card h-fit p-5"><h2 className="mb-4 font-bold">Add available player</h2><form action={addWaiverCandidate} className="space-y-3"><input type="hidden" name="teamId" value={team.id}/><label className="field">Name<input name="name" required/></label><div className="grid grid-cols-2 gap-2"><label className="field">Team<input name="nflTeam" maxLength={3} required/></label><label className="field">Position<select name="position">{["QB","RB","WR","TE","K","DST"].map(x=><option key={x}>{x}</option>)}</select></label></div>{[["projectedPoints","Projection",10],["floorPoints","Floor",5],["ceilingPoints","Ceiling",18],["restOfSeasonPoints","ROS points",80],["consistency","Consistency",.5],["matchupRating","Matchup",0],["injuryMultiplier","Health",1]].map(([name,label,value])=><label className="field" key={String(name)}>{label}<input name={String(name)} type="number" step="0.1" defaultValue={value}/></label>)}<FormSubmit className="button-primary w-full">Add candidate</FormSubmit></form></aside></div></main> }
